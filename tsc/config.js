module.exports = {
  // Change `seRoot` to the correct path to the Skyrim SE folder. The path should have slashes like this: `/` (not `//`).
  seRoot: "F:/SteamLibrary/steamapps/common/Skyrim Special Edition",
}
